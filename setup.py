import os
from setuptools import setup, find_packages

module_name = "Housing market places scrapers"

setup(
    name=module_name,
    version="0.1.0",
    author="Simon Vreugdenhil",
    author_email="simon@simonvreugdenhil.nl",
    packages=find_packages()
    )
